# Pencil Logan P1 Sample

Cross-product isolation fixture — must never appear when scoped to
`product=iphone` with the same `project=logan` / `build=p1` slugs.

- pencil-only marker: PENCIL_LOGAN_P1_SECRET
- designator twin: U101
